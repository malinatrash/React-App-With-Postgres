const {useState} = require("react");
const useButtonDisabled = (name, lastname, phone) => {
    const [fistName, setFirstName] = useState(name)
    const [lastName, setLastName] = useState(lastname)
    const [phoneNumber, setPhone] = useState(phone)


}
